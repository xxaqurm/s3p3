server:
  host: "127.0.0.1"
  port: 8080

database:
  name: "security_db"
  collection: "security_events.json"

logging:
  agent_id: "agent-ubuntu-01"
  sources:
    - auditd
    - syslog
    - bash_history
  send_interval: 30
  batch_size: 100
